{
  "name": "cups_codes",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "package": "rmdir /s /q dist && npm run build && cp -r -t dist node_modules cups_codes.json feature_names.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.592.0",
    "@types/aws-lambda": "^8.10.138"
  },
  "description": "",
  "devDependencies": {
    "@types/node": "^22.5.5"
  }
}
